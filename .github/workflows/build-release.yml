name: Build and Release

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    uses: asita88/ci-templates/.github/workflows/build-release-openresty.yml@main
    permissions:
      contents: write
    with:
      app_name: apiok
      build_script: scripts/build.sh
    secrets: inherit
